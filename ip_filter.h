#pragma once

#include <vector>
#include <string>

std::vector<std::string> Split(const std::string &str, char d);

std::vector<std::vector<int>> GetFinalResult(std::vector<std::vector<int>>& ipPool);